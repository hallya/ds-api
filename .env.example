# Synology Download Station API Configuration
# Copy this file to .env and configure your settings

# Required: Synology NAS URL
NAS_URL=https://your-nas-url

# Required: Synology credentials
SYNOLOGY_USERNAME=your_username
SYNOLOGY_PASSWORD=your_password_here

# Required: Base path for file operations
SYNOLOGY_BASE_PATH=/volume1

# Optional: Disable SSL verification for self-signed certificates (default: false)
# SYNOLOGY_DISABLE_SSL_VERIFICATION=false

# Optional: Logging level - ERROR, WARN, INFO, DEBUG (default: INFO)
# LOG_LEVEL=INFO

# Optional: Number of retry attempts for API calls (default: 3)
# RETRY_ATTEMPTS=3

# Optional: Delay between retry attempts in milliseconds (default: 1000)
# RETRY_DELAY=1000
